DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=fleet.thermofamerp.com.et,127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=https://fleet.thermofamerp.com.et

# Django secret key (set your own strong value)
SECRET_KEY=replace_me_with_a_strong_random_value

# Database (aligned with repo defaults)
DB_NAME=fleet
DB_USER=admin
DB_PASSWORD=Admin_thermo
DB_HOST=127.0.0.1
DB_PORT=3306

# Email (optional – override if needed)
EMAIL_HOST_USER=no.reply.thermofam@gmail.com
EMAIL_HOST_PASSWORD=update_or_store_in_app_password
